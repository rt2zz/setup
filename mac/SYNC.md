# Setup launch agent
1. `cp ./_dev.zee.launch.plist ~/Library/LaunchAgents/dev.zee.launch.plist`
2. `launchctl load ~/Library/LaunchAgents/dev.zee.launch.plist`

Notes:
- if the script is not executable `chmod +x ~/dev/setup/periodic.sh`
